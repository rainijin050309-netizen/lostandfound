<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lostandfound.lostandfound.claim.mapper.ClaimMapper">

    <resultMap id="ClaimResultMap" type="com.lostandfound.lostandfound.claim.entity.Claim">
        <id property="id" column="id"/>
        <result property="itemId" column="itemId"/>
        <result property="userId" column="userId"/>
        <result property="message" column="message"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="createdAt"/>
        <result property="updatedAt" column="updatedAt"/>
    </resultMap>

    <select id="findByItemId" resultMap="ClaimResultMap">
        SELECT *
        FROM claim
        WHERE itemId = #{itemId}
        ORDER BY createdAt DESC
    </select>

    <select id="findByUserId" resultMap="ClaimResultMap">
        SELECT *
        FROM claim
        WHERE userId = #{userId}
        ORDER BY createdAt DESC
    </select>

    <select id="countByItemIdAndUserId" resultType="long">
        SELECT COUNT(*)
        FROM claim
        WHERE itemId = #{itemId}
          AND userId = #{userId}
    </select>

    <update id="rejectOtherClaims">
        UPDATE claim
        SET status = 'rejected'
        WHERE itemId = #{itemId}
          AND id != #{excludeId}
          AND status = 'pending'
    </update>

    <delete id="deleteByItemId">
        DELETE FROM claim
        WHERE itemId = #{itemId}
    </delete>

</mapper>