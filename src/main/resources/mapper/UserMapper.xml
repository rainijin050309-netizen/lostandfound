<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lostandfound.lostandfound.user.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.lostandfound.lostandfound.user.entity.User">
        <id property="id" column="id"/>
        <result property="studentId" column="studentId"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="createdAt"/>
        <result property="updatedAt" column="updatedAt"/>
    </resultMap>

    <select id="findByStudentIdAndPassword" resultMap="UserResultMap">
        SELECT *
        FROM user
        WHERE studentId = #{studentId}
          AND password = #{password}
    </select>

    <select id="findByStudentId" resultMap="UserResultMap">
        SELECT *
        FROM user
        WHERE studentId = #{studentId}
    </select>

    <select id="countByStudentId" resultType="long">
        SELECT COUNT(*)
        FROM user
        WHERE studentId = #{studentId}
    </select>

</mapper>